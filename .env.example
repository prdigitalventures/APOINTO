# Local / hosted Postgres (Neon, Railway Postgres, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"
# Direct (non-pooled) connection — required for Prisma migrations
DIRECT_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"
JWT_SECRET="change-this-in-production"
OPENAI_API_KEY=""

# Public site URL used in verification / reset emails
APP_URL="https://www.apointo.online"

# Google OAuth (customers). If these are empty, the Google button still appears
# but sign-in shows a setup message.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="https://www.apointo.online/api/auth/google/callback"

# Email delivery — verification and password reset links (no SMS OTP).
# Prefer Resend; SMTP is used if Resend is not set. If neither is configured,
# tokens are still created and the full URL is written to server logs.
RESEND_API_KEY=""
EMAIL_FROM="Apointo <noreply@apointo.online>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE="false"
SMTP_FROM=""
